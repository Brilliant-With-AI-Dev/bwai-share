# Chat goals (orchestrator fills each run)

Write 3–5 bullets before critic subagents or silent `auto-improve`:

1. What Adrian asked for in this session (task + artifact path if known)
2. Primary reader / audience
3. What success looks like (ship, draft for Matt, client email, etc.)
4. Explicit constraints from chat (do not break URL, Tev-only, etc.)
5. Optional: what this artifact is *not*

If chain started from `copy-work.mdc` with thin chat context, derive from **latest user
message + open file path**.

Store inline in critic prompt or a temp note — subagents do not inherit full chat history.
