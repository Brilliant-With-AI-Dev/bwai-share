# LIGHT Example

Input plan: update one settings button label and add a snapshot test.

Posture: approve fast if it checks existing copy conventions; at most one fix.

Example output (clean):

```
COMPLEXITY: LIGHT   RECO: GO
No fixes. GO.
```
