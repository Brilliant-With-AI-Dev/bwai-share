# Deterministic work belongs to code; intent needs judgment

Matt correctly explained that tests belong in code phases because they are deterministic, while evaluating intent is not deterministic and therefore belongs in an agent phase. This establishes the core ownership rule needed to read and customize ADWs.

**Evidence:** In retrieval practice, Matt answered: “tests are deterministic, intent is not.”
